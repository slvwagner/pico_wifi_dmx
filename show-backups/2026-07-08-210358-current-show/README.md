# Current Show Backup - 2026-07-08 21:03:58

This folder captures the live XAMPP show state from:

```text
E:\Software\xampp\htdocs\dmx\data
```

Use `pico_dmx_setup.json` with **Fixture Controller > Show > Import Setup** to restore the show through the app.

`raw-data/` contains the copied XAMPP JSON files for inspection or manual restore into an XAMPP `data` folder.

`backup-manifest.json` records file sizes and SHA256 hashes for the copied files.
